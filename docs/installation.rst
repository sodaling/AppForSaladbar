============
Installation
============

At the command line::

    $ easy_install AppForSaladbar

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv AppForSaladbar
    $ pip install AppForSaladbar
